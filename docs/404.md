---
title: Page Not Found
layout: page
head:
  - - script
    - {}
    - |
      ;(() => {
        const { pathname, search, hash } = window.location

        if (pathname !== '/' && pathname.endsWith('/')) {
          window.location.replace(`${pathname}index.html${search}${hash}`)
        }
      })()
---

# Page Not Found

The page you requested does not exist.

[Go back home](/)
